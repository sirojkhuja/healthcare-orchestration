openapi: 3.1.1
info:
  title: MedFlow Scheduling and Clinical Fragment
  version: 0.1.0
paths:
  /api/v1/appointments:
    get:
      operationId: ListAppointmentsQuery
      tags: [Appointments]
      summary: List active appointments in the current tenant
      description: Returns active tenant-scoped appointments ordered by scheduled start time.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Appointment directory
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentCollectionResponse'
              examples:
                default:
                  value:
                    data:
                      - id: 6b565215-58f5-45b2-a89d-292c489b21c1
                        tenant_id: 50234272-f1a8-40e8-a548-3a3b60230936
                        status: draft
                        patient:
                          id: 2c9fcd89-f6c6-49bf-a22c-476b32e9be6e
                          display_name: Aziza Karimova
                        provider:
                          id: 0cf87d48-8892-4d43-8d50-7b1046adc80f
                          display_name: Kamola Rasulova
                        clinic: null
                        room: null
                        scheduled_slot:
                          start_at: '2026-03-13T04:00:00+00:00'
                          end_at: '2026-03-13T04:30:00+00:00'
                          timezone: Asia/Tashkent
                        last_transition: null
                        deleted_at: null
                        created_at: '2026-03-10T08:00:00+00:00'
                        updated_at: '2026-03-10T08:00:00+00:00'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    post:
      operationId: CreateAppointmentCommand
      tags: [Appointments]
      summary: Create a draft appointment
      description: Creates a tenant-owned appointment in draft status.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAppointmentRequest'
            examples:
              default:
                value:
                  patient_id: 2c9fcd89-f6c6-49bf-a22c-476b32e9be6e
                  provider_id: 0cf87d48-8892-4d43-8d50-7b1046adc80f
                  clinic_id: 52ccdb45-a06d-47dd-a2ea-e39126402f61
                  room_id: 68116f2b-c8e6-44c9-8941-ff995f301db1
                  scheduled_start_at: '2026-03-13T09:00:00+05:00'
                  scheduled_end_at: '2026-03-13T09:30:00+05:00'
                  timezone: Asia/Tashkent
      responses:
        '201':
          description: Draft appointment created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid payload or cross-reference mismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/search:
    get:
      operationId: SearchAppointmentsQuery
      tags: [Appointments]
      summary: Search active appointments in tenant scope
      description: Searches appointment IDs plus patient and provider display names with structured tenant-scoped filters.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/AppointmentSearchQuery'
        - $ref: '#/components/parameters/AppointmentStatusFilter'
        - $ref: '#/components/parameters/AppointmentPatientFilter'
        - $ref: '#/components/parameters/AppointmentProviderFilter'
        - $ref: '#/components/parameters/AppointmentClinicFilter'
        - $ref: '#/components/parameters/AppointmentRoomFilter'
        - $ref: '#/components/parameters/AppointmentScheduledFromFilter'
        - $ref: '#/components/parameters/AppointmentScheduledToFilter'
        - $ref: '#/components/parameters/AppointmentCreatedFromFilter'
        - $ref: '#/components/parameters/AppointmentCreatedToFilter'
        - $ref: '#/components/parameters/AppointmentSearchLimit'
      responses:
        '200':
          description: Matching appointments ordered by relevance
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentSearchResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid search filter combination
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/export:
    get:
      operationId: ExportAppointmentsQuery
      tags: [Appointments]
      summary: Export the active appointment search result set
      description: Generates a CSV snapshot of the active appointment search result set and stores it on the private exports disk.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/AppointmentSearchQuery'
        - $ref: '#/components/parameters/AppointmentStatusFilter'
        - $ref: '#/components/parameters/AppointmentPatientFilter'
        - $ref: '#/components/parameters/AppointmentProviderFilter'
        - $ref: '#/components/parameters/AppointmentClinicFilter'
        - $ref: '#/components/parameters/AppointmentRoomFilter'
        - $ref: '#/components/parameters/AppointmentScheduledFromFilter'
        - $ref: '#/components/parameters/AppointmentScheduledToFilter'
        - $ref: '#/components/parameters/AppointmentCreatedFromFilter'
        - $ref: '#/components/parameters/AppointmentCreatedToFilter'
        - $ref: '#/components/parameters/AppointmentExportLimit'
        - $ref: '#/components/parameters/AppointmentExportFormat'
      responses:
        '200':
          description: Export generated and persisted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentExportResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid export filter combination
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/bulk:
    post:
      operationId: BulkUpdateAppointmentsCommand
      tags: [Appointments]
      summary: Apply one shared change set to multiple draft appointments
      description: Updates only active draft appointments in the current tenant. The request is all-or-nothing and requires an idempotency key.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkUpdateAppointmentsRequest'
            examples:
              default:
                value:
                  appointment_ids:
                    - 6b565215-58f5-45b2-a89d-292c489b21c1
                    - 0bf4ae31-f5d4-406d-88a5-84bde8209a5f
                  changes:
                    timezone: UTC
      responses:
        '200':
          description: Draft appointments updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkAppointmentUpdateResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: One or more appointments do not exist in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: One or more selected appointments are not in draft status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid payload or unsupported change set
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments:bulk-cancel:
    post:
      operationId: BulkCancelAppointmentsCommand
      tags: [Appointments]
      summary: Cancel multiple scheduled or confirmed appointments
      description: Cancels `1..100` active tenant-scoped appointments in one all-or-nothing workflow mutation.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkCancelAppointmentsRequest'
      responses:
        '200':
          description: Appointments canceled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkAppointmentTransitionResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: One or more appointments do not exist in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: One or more selected appointments cannot be canceled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments:bulk-reschedule:
    post:
      operationId: BulkRescheduleAppointmentsCommand
      tags: [Appointments]
      summary: Reschedule multiple scheduled or confirmed appointments
      description: Reschedules `1..100` active tenant-scoped appointments in one all-or-nothing workflow mutation and returns both source and replacement appointments.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkRescheduleAppointmentsRequest'
      responses:
        '200':
          description: Appointments rescheduled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkAppointmentTransitionResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: One or more appointments do not exist in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: One or more selected appointments cannot be rescheduled or a replacement slot is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}:schedule:
    post:
      operationId: ScheduleAppointmentCommand
      tags: [Appointments]
      summary: Schedule one draft appointment
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '200':
          description: Appointment scheduled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentWorkflowMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Appointment cannot be scheduled or the slot is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}:confirm:
    post:
      operationId: ConfirmAppointmentCommand
      tags: [Appointments]
      summary: Confirm one scheduled appointment
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '200':
          description: Appointment confirmed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentWorkflowMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Appointment cannot be confirmed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}:check-in:
    post:
      operationId: CheckInAppointmentCommand
      tags: [Appointments]
      summary: Check in one appointment
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppointmentCheckInRequest'
      responses:
        '200':
          description: Appointment checked in
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentWorkflowMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Appointment cannot be checked in
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}:start:
    post:
      operationId: StartAppointmentCommand
      tags: [Appointments]
      summary: Start one checked-in appointment
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '200':
          description: Appointment started
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentWorkflowMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Appointment cannot be started
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}:complete:
    post:
      operationId: CompleteAppointmentCommand
      tags: [Appointments]
      summary: Complete one in-progress appointment
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '200':
          description: Appointment completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentWorkflowMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Appointment cannot be completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}:cancel:
    post:
      operationId: CancelAppointmentCommand
      tags: [Appointments]
      summary: Cancel one scheduled or confirmed appointment
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppointmentReasonRequest'
      responses:
        '200':
          description: Appointment canceled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentWorkflowMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Appointment cannot be canceled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid cancellation payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}:no-show:
    post:
      operationId: MarkNoShowCommand
      tags: [Appointments]
      summary: Mark one scheduled or confirmed appointment as no-show
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppointmentReasonRequest'
      responses:
        '200':
          description: Appointment marked as no-show
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentWorkflowMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Appointment cannot be marked as no-show
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid no-show payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}:reschedule:
    post:
      operationId: RescheduleAppointmentCommand
      tags: [Appointments]
      summary: Reschedule one scheduled or confirmed appointment
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppointmentRescheduleRequest'
      responses:
        '200':
          description: Appointment rescheduled and replacement appointment created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentRescheduleResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Appointment cannot be rescheduled or the replacement slot is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid reschedule payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}:restore:
    post:
      operationId: RestoreAppointmentCommand
      tags: [Appointments]
      summary: Restore one canceled, no-show, or rescheduled appointment
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '200':
          description: Appointment restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentWorkflowMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Appointment cannot be restored or the original slot is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}:make-recurring:
    post:
      operationId: MakeAppointmentRecurringCommand
      tags: [Appointments]
      summary: Materialize a recurring series from one scheduled or confirmed appointment
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecurringAppointmentRequest'
      responses:
        '201':
          description: Recurrence created and future appointments materialized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentRecurrenceMaterializationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Appointment cannot become recurring or a future slot is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid recurrence payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/recurrences/{recurrenceId}:cancel:
    post:
      operationId: CancelRecurrenceCommand
      tags: [Appointments]
      summary: Cancel one active appointment recurrence
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/RecurrenceIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AppointmentReasonRequest'
      responses:
        '200':
          description: Recurrence canceled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentRecurrenceMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Recurrence not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Recurrence cannot be canceled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid recurrence cancellation payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/waitlist:
    get:
      operationId: ListWaitlistQuery
      tags: [Appointments]
      summary: List waitlist entries in the current tenant
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/WaitlistStatusFilter'
        - $ref: '#/components/parameters/WaitlistPatientFilter'
        - $ref: '#/components/parameters/WaitlistProviderFilter'
        - $ref: '#/components/parameters/WaitlistClinicFilter'
        - $ref: '#/components/parameters/WaitlistDesiredFromFilter'
        - $ref: '#/components/parameters/WaitlistDesiredToFilter'
        - $ref: '#/components/parameters/WaitlistLimit'
      responses:
        '200':
          description: Waitlist entries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WaitlistCollectionResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid waitlist filter combination
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    post:
      operationId: AddToWaitlistCommand
      tags: [Appointments]
      summary: Add one patient to the waitlist
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WaitlistCreateRequest'
      responses:
        '201':
          description: Waitlist entry created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WaitlistMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid waitlist payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/waitlist/{entryId}:
    delete:
      operationId: RemoveFromWaitlistCommand
      tags: [Appointments]
      summary: Remove one open waitlist entry
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/WaitlistEntryIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '200':
          description: Waitlist entry removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WaitlistMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Waitlist entry not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Waitlist entry cannot be removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/waitlist/{entryId}:offer-slot:
    post:
      operationId: OfferWaitlistSlotCommand
      tags: [Appointments]
      summary: Offer one available slot to an open waitlist entry
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/WaitlistEntryIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WaitlistOfferRequest'
      responses:
        '200':
          description: Waitlist entry booked and appointment created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WaitlistOfferResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Waitlist entry not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Waitlist entry cannot be booked or the slot is unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid offered slot payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}:
    get:
      operationId: GetAppointmentQuery
      tags: [Appointments]
      summary: Get one active appointment in tenant scope
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Appointment detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    patch:
      operationId: UpdateAppointmentCommand
      tags: [Appointments]
      summary: Update a draft appointment
      description: Updates only draft appointments. Workflow state changes remain on explicit action routes.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAppointmentRequest'
            examples:
              default:
                value:
                  scheduled_end_at: '2026-03-13T09:45:00+05:00'
      responses:
        '200':
          description: Draft appointment updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Appointment is no longer mutable through generic CRUD
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid payload or cross-reference mismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    delete:
      operationId: DeleteAppointmentCommand
      tags: [Appointments]
      summary: Soft-delete a draft appointment
      description: Soft-deletes only draft appointments. Deleted drafts leave active reads but remain auditable.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '200':
          description: Draft appointment soft-deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Appointment is no longer deletable through generic CRUD
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}/participants:
    get:
      operationId: ListAppointmentParticipantsQuery
      tags: [Appointments]
      summary: List appointment participants
      description: Returns appointment participants for an active tenant-scoped appointment ordered by required flag, display name, and creation time.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Appointment participants
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentParticipantCollectionResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    post:
      operationId: AddAppointmentParticipantCommand
      tags: [Appointments]
      summary: Add one appointment participant
      description: Adds one participant to an active tenant-scoped appointment and snapshots the participant display name.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAppointmentParticipantRequest'
      responses:
        '201':
          description: Appointment participant created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentParticipantMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: The appointment already contains that participant
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid participant payload or unresolved reference
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}/participants/{participantId}:
    delete:
      operationId: RemoveAppointmentParticipantCommand
      tags: [Appointments]
      summary: Remove one appointment participant
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/AppointmentParticipantIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '200':
          description: Appointment participant removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentParticipantMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment or participant not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}/notes:
    get:
      operationId: ListAppointmentNotesQuery
      tags: [Appointments]
      summary: List appointment notes
      description: Returns appointment notes for an active tenant-scoped appointment ordered by most recently updated first.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Appointment notes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentNoteCollectionResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    post:
      operationId: AddAppointmentNoteCommand
      tags: [Appointments]
      summary: Add one appointment note
      description: Adds an authored note to an active tenant-scoped appointment and snapshots the authenticated author identity.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAppointmentNoteRequest'
      responses:
        '201':
          description: Appointment note created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentNoteMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid note payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}/notes/{noteId}:
    patch:
      operationId: UpdateAppointmentNoteCommand
      tags: [Appointments]
      summary: Update one appointment note
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/AppointmentNoteIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAppointmentNoteRequest'
      responses:
        '200':
          description: Appointment note updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentNoteMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment or note not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid note payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    delete:
      operationId: DeleteAppointmentNoteCommand
      tags: [Appointments]
      summary: Delete one appointment note
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/AppointmentNoteIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      responses:
        '200':
          description: Appointment note deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentNoteMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment or note not found in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/appointments/{appointmentId}/audit:
    get:
      operationId: GetAppointmentAuditQuery
      tags: [Appointments]
      summary: Get appointment audit history
      description: Returns immutable appointment audit events newest first and remains available for soft-deleted appointments in tenant scope.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/AppointmentIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/AppointmentAuditLimit'
      responses:
        '200':
          description: Appointment audit history
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppointmentAuditResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or appointment read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Appointment not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/treatment-plans:
    get:
      operationId: ListTreatmentPlansQuery
      tags: [Treatment Plans]
      summary: List active treatment plans in the current tenant
      description: Returns active tenant-scoped treatment plans ordered newest first and includes the current treatment item count per plan.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Treatment plan directory
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanCollectionResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or treatment-plan read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    post:
      operationId: CreateTreatmentPlanCommand
      tags: [Treatment Plans]
      summary: Create a draft treatment plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTreatmentPlanRequest'
      responses:
        '201':
          description: Draft treatment plan created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or treatment-plan write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid payload or cross-reference mismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/treatment-plans/search:
    get:
      operationId: SearchTreatmentPlansQuery
      tags: [Treatment Plans]
      summary: Search active treatment plans in tenant scope
      description: Searches treatment-plan ids, titles, patient names, provider names, and treatment-item titles with structured tenant-scoped filters.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/TreatmentPlanSearchQuery'
        - $ref: '#/components/parameters/TreatmentPlanStatusFilter'
        - $ref: '#/components/parameters/TreatmentPlanPatientFilter'
        - $ref: '#/components/parameters/TreatmentPlanProviderFilter'
        - $ref: '#/components/parameters/TreatmentPlanPlannedFromFilter'
        - $ref: '#/components/parameters/TreatmentPlanPlannedToFilter'
        - $ref: '#/components/parameters/TreatmentPlanCreatedFromFilter'
        - $ref: '#/components/parameters/TreatmentPlanCreatedToFilter'
        - $ref: '#/components/parameters/TreatmentPlanSearchLimit'
      responses:
        '200':
          description: Matching treatment plans ordered newest first
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanSearchResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or treatment-plan read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid search filter combination
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/treatment-plans/{planId}:
    get:
      operationId: GetTreatmentPlanQuery
      tags: [Treatment Plans]
      summary: Get one treatment plan in tenant scope
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Treatment plan payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or treatment-plan read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Treatment plan not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    patch:
      operationId: UpdateTreatmentPlanCommand
      tags: [Treatment Plans]
      summary: Update a draft or approved treatment plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTreatmentPlanRequest'
      responses:
        '200':
          description: Treatment plan updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or treatment-plan write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Treatment plan not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Treatment plan is no longer editable in its current lifecycle state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid patch payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    delete:
      operationId: DeleteTreatmentPlanCommand
      tags: [Treatment Plans]
      summary: Soft-delete a draft or rejected treatment plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Treatment plan deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or treatment-plan write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Treatment plan not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Treatment plan is not deletable in its current lifecycle state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/treatment-plans/{planId}/items:
    get:
      operationId: ListTreatmentItemsQuery
      tags: [Treatment Plans]
      summary: List ordered treatment items for one plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Ordered treatment items
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanItemCollectionResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or treatment-plan read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Treatment plan not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    post:
      operationId: AddTreatmentItemCommand
      tags: [Treatment Plans]
      summary: Add one ordered treatment item to a draft or approved plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTreatmentPlanItemRequest'
      responses:
        '201':
          description: Treatment item created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanItemMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or treatment-plan write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Treatment plan not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Treatment items are read-only in the current plan lifecycle state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid treatment-item payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/treatment-plans/{planId}/items/{itemId}:
    patch:
      operationId: UpdateTreatmentItemCommand
      tags: [Treatment Plans]
      summary: Update one ordered treatment item inside a draft or approved plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TreatmentPlanItemIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTreatmentPlanItemRequest'
      responses:
        '200':
          description: Treatment item updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanItemMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or treatment-plan write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Treatment plan or item not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Treatment items are read-only in the current plan lifecycle state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid treatment-item patch payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    delete:
      operationId: RemoveTreatmentItemCommand
      tags: [Treatment Plans]
      summary: Delete one ordered treatment item from a draft or approved plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TreatmentPlanItemIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Treatment item deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanItemMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or treatment-plan write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Treatment plan or item not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Treatment items are read-only in the current plan lifecycle state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/treatment-plans/{planId}:approve:
    post:
      operationId: ApproveTreatmentPlanCommand
      tags: [Treatment Plans]
      summary: Approve a draft treatment plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Treatment plan approved
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanMutationResponse'
  /api/v1/treatment-plans/{planId}:start:
    post:
      operationId: StartTreatmentPlanCommand
      tags: [Treatment Plans]
      summary: Start an approved treatment plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Treatment plan started
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanMutationResponse'
  /api/v1/treatment-plans/{planId}:pause:
    post:
      operationId: PauseTreatmentPlanCommand
      tags: [Treatment Plans]
      summary: Pause an active treatment plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TreatmentPlanReasonRequest'
      responses:
        '200':
          description: Treatment plan paused
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanMutationResponse'
  /api/v1/treatment-plans/{planId}:resume:
    post:
      operationId: ResumeTreatmentPlanCommand
      tags: [Treatment Plans]
      summary: Resume a paused treatment plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Treatment plan resumed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanMutationResponse'
  /api/v1/treatment-plans/{planId}:finish:
    post:
      operationId: FinishTreatmentPlanCommand
      tags: [Treatment Plans]
      summary: Finish an active or paused treatment plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Treatment plan finished
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanMutationResponse'
  /api/v1/treatment-plans/{planId}:reject:
    post:
      operationId: RejectTreatmentPlanCommand
      tags: [Treatment Plans]
      summary: Reject a draft or approved treatment plan
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TreatmentPlanIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TreatmentPlanReasonRequest'
      responses:
        '200':
          description: Treatment plan rejected
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreatmentPlanMutationResponse'
  /api/v1/encounters:
    get:
      operationId: ListEncountersQuery
      tags: [Encounters]
      summary: List active encounters in the current tenant
      description: Returns active tenant-scoped encounters with structured filters and linked diagnosis and procedure match support.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/EncounterSearchQuery'
        - $ref: '#/components/parameters/EncounterStatusFilter'
        - $ref: '#/components/parameters/EncounterPatientFilter'
        - $ref: '#/components/parameters/EncounterProviderFilter'
        - $ref: '#/components/parameters/EncounterTreatmentPlanFilter'
        - $ref: '#/components/parameters/EncounterAppointmentFilter'
        - $ref: '#/components/parameters/EncounterClinicFilter'
        - $ref: '#/components/parameters/EncounterFromFilter'
        - $ref: '#/components/parameters/EncounterToFilter'
        - $ref: '#/components/parameters/EncounterCreatedFromFilter'
        - $ref: '#/components/parameters/EncounterCreatedToFilter'
        - $ref: '#/components/parameters/EncounterSearchLimit'
      responses:
        '200':
          description: Encounter directory
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EncounterCollectionResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid encounter filter combination
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    post:
      operationId: CreateEncounterCommand
      tags: [Encounters]
      summary: Create an open encounter
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEncounterRequest'
      responses:
        '201':
          description: Encounter created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EncounterMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid payload or cross-reference mismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/encounters/export:
    get:
      operationId: ExportEncountersQuery
      tags: [Encounters]
      summary: Export the active encounter result set
      description: Generates a CSV snapshot of the active encounter filter result set and stores it on the private exports disk.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/EncounterSearchQuery'
        - $ref: '#/components/parameters/EncounterStatusFilter'
        - $ref: '#/components/parameters/EncounterPatientFilter'
        - $ref: '#/components/parameters/EncounterProviderFilter'
        - $ref: '#/components/parameters/EncounterTreatmentPlanFilter'
        - $ref: '#/components/parameters/EncounterAppointmentFilter'
        - $ref: '#/components/parameters/EncounterClinicFilter'
        - $ref: '#/components/parameters/EncounterFromFilter'
        - $ref: '#/components/parameters/EncounterToFilter'
        - $ref: '#/components/parameters/EncounterCreatedFromFilter'
        - $ref: '#/components/parameters/EncounterCreatedToFilter'
        - $ref: '#/components/parameters/EncounterExportLimit'
        - $ref: '#/components/parameters/EncounterExportFormat'
      responses:
        '200':
          description: Encounter export generated and persisted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EncounterExportResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid encounter export filter combination
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/encounters/bulk:
    post:
      operationId: BulkUpdateEncountersCommand
      tags: [Encounters]
      summary: Apply one shared change set to multiple encounters
      description: Updates active encounters in the current tenant with one shared all-or-nothing change set. The route requires an idempotency key.
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/TenantIdHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkUpdateEncountersRequest'
      responses:
        '200':
          description: Encounters updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkEncounterUpdateResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: One or more encounters do not exist in the active tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid bulk encounter payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/encounters/{encounterId}:
    get:
      operationId: GetEncounterQuery
      tags: [Encounters]
      summary: Get one encounter in tenant scope
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/EncounterIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Encounter payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EncounterResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Encounter not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    patch:
      operationId: UpdateEncounterCommand
      tags: [Encounters]
      summary: Update one encounter
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/EncounterIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateEncounterRequest'
      responses:
        '200':
          description: Encounter updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EncounterMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Encounter not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid encounter patch payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    delete:
      operationId: DeleteEncounterCommand
      tags: [Encounters]
      summary: Soft-delete an open or entered-in-error encounter
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/EncounterIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Encounter deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EncounterMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Encounter not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Encounter is not deletable in its current status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/encounters/{encounterId}/diagnoses:
    get:
      operationId: ListDiagnosesQuery
      tags: [Encounters]
      summary: List diagnoses for one encounter
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/EncounterIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Encounter diagnoses ordered primary first
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EncounterDiagnosisCollectionResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Encounter not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    post:
      operationId: AddDiagnosisCommand
      tags: [Encounters]
      summary: Add one diagnosis to an encounter
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/EncounterIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEncounterDiagnosisRequest'
      responses:
        '201':
          description: Diagnosis added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EncounterDiagnosisMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Encounter not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Duplicate diagnosis or primary diagnosis rule violation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid diagnosis payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/encounters/{encounterId}/diagnoses/{dxId}:
    delete:
      operationId: RemoveDiagnosisCommand
      tags: [Encounters]
      summary: Remove one diagnosis from an encounter
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/EncounterIdPath'
        - $ref: '#/components/parameters/EncounterDiagnosisIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Diagnosis removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EncounterDiagnosisMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Encounter or diagnosis not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/encounters/{encounterId}/procedures:
    get:
      operationId: ListProceduresQuery
      tags: [Encounters]
      summary: List procedures for one encounter
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/EncounterIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Encounter procedures ordered by performed time
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EncounterProcedureCollectionResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter read permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Encounter not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
    post:
      operationId: AddProcedureCommand
      tags: [Encounters]
      summary: Add one procedure to an encounter
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/EncounterIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEncounterProcedureRequest'
      responses:
        '201':
          description: Procedure added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EncounterProcedureMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Encounter not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '409':
          description: Duplicate procedure
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '422':
          description: Invalid procedure payload
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
  /api/v1/encounters/{encounterId}/procedures/{procId}:
    delete:
      operationId: RemoveProcedureCommand
      tags: [Encounters]
      summary: Remove one procedure from an encounter
      security:
        - bearerAuth: []
      parameters:
        - $ref: '#/components/parameters/EncounterIdPath'
        - $ref: '#/components/parameters/EncounterProcedureIdPath'
        - $ref: '#/components/parameters/TenantIdHeader'
      responses:
        '200':
          description: Procedure removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EncounterProcedureMutationResponse'
        '401':
          description: Missing or invalid bearer token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '403':
          description: Missing tenant scope or encounter write permission
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
        '404':
          description: Encounter or procedure not found in tenant scope
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiErrorResponse'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    TenantIdHeader:
      in: header
      name: X-Tenant-Id
      required: true
      schema:
        type: string
        format: uuid
      description: Active tenant scope for tenant-owned operations in this fragment.
    IdempotencyKeyHeader:
      in: header
      name: Idempotency-Key
      required: true
      schema:
        type: string
        minLength: 8
        maxLength: 255
      description: Required for protected mutation routes that opt into the shared idempotency contract. The uniqueness scope is tenant plus operation plus key.
    AppointmentIdPath:
      in: path
      name: appointmentId
      required: true
      schema:
        type: string
        format: uuid
    TreatmentPlanIdPath:
      in: path
      name: planId
      required: true
      schema:
        type: string
        format: uuid
    TreatmentPlanItemIdPath:
      in: path
      name: itemId
      required: true
      schema:
        type: string
        format: uuid
    EncounterIdPath:
      in: path
      name: encounterId
      required: true
      schema:
        type: string
        format: uuid
    EncounterDiagnosisIdPath:
      in: path
      name: dxId
      required: true
      schema:
        type: string
        format: uuid
    EncounterProcedureIdPath:
      in: path
      name: procId
      required: true
      schema:
        type: string
        format: uuid
    AppointmentParticipantIdPath:
      in: path
      name: participantId
      required: true
      schema:
        type: string
        format: uuid
    AppointmentNoteIdPath:
      in: path
      name: noteId
      required: true
      schema:
        type: string
        format: uuid
    RecurrenceIdPath:
      in: path
      name: recurrenceId
      required: true
      schema:
        type: string
        format: uuid
    WaitlistEntryIdPath:
      in: path
      name: entryId
      required: true
      schema:
        type: string
        format: uuid
    AppointmentSearchQuery:
      in: query
      name: q
      required: false
      schema:
        type: string
        maxLength: 120
    AppointmentStatusFilter:
      in: query
      name: status
      required: false
      schema:
        type: string
        enum: [draft, scheduled, confirmed, checked_in, in_progress, completed, canceled, no_show, rescheduled]
    AppointmentPatientFilter:
      in: query
      name: patient_id
      required: false
      schema:
        type: string
        format: uuid
    AppointmentProviderFilter:
      in: query
      name: provider_id
      required: false
      schema:
        type: string
        format: uuid
    AppointmentClinicFilter:
      in: query
      name: clinic_id
      required: false
      schema:
        type: string
        format: uuid
    AppointmentRoomFilter:
      in: query
      name: room_id
      required: false
      schema:
        type: string
        format: uuid
    AppointmentScheduledFromFilter:
      in: query
      name: scheduled_from
      required: false
      schema:
        type: string
        format: date
    AppointmentScheduledToFilter:
      in: query
      name: scheduled_to
      required: false
      schema:
        type: string
        format: date
    AppointmentCreatedFromFilter:
      in: query
      name: created_from
      required: false
      schema:
        type: string
        format: date
    AppointmentCreatedToFilter:
      in: query
      name: created_to
      required: false
      schema:
        type: string
        format: date
    AppointmentSearchLimit:
      in: query
      name: limit
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    AppointmentExportLimit:
      in: query
      name: limit
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 1000
        default: 1000
    AppointmentExportFormat:
      in: query
      name: format
      required: false
      schema:
        type: string
        enum: [csv]
        default: csv
    TreatmentPlanSearchQuery:
      in: query
      name: q
      required: false
      schema:
        type: string
        maxLength: 120
    TreatmentPlanStatusFilter:
      in: query
      name: status
      required: false
      schema:
        type: string
        enum: [draft, approved, active, paused, finished, rejected]
    TreatmentPlanPatientFilter:
      in: query
      name: patient_id
      required: false
      schema:
        type: string
        format: uuid
    TreatmentPlanProviderFilter:
      in: query
      name: provider_id
      required: false
      schema:
        type: string
        format: uuid
    TreatmentPlanPlannedFromFilter:
      in: query
      name: planned_from
      required: false
      schema:
        type: string
        format: date
    TreatmentPlanPlannedToFilter:
      in: query
      name: planned_to
      required: false
      schema:
        type: string
        format: date
    TreatmentPlanCreatedFromFilter:
      in: query
      name: created_from
      required: false
      schema:
        type: string
        format: date
    TreatmentPlanCreatedToFilter:
      in: query
      name: created_to
      required: false
      schema:
        type: string
        format: date
    TreatmentPlanSearchLimit:
      in: query
      name: limit
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    EncounterSearchQuery:
      in: query
      name: q
      required: false
      schema:
        type: string
        maxLength: 120
    EncounterStatusFilter:
      in: query
      name: status
      required: false
      schema:
        type: string
        enum: [open, completed, entered_in_error]
    EncounterPatientFilter:
      in: query
      name: patient_id
      required: false
      schema:
        type: string
        format: uuid
    EncounterProviderFilter:
      in: query
      name: provider_id
      required: false
      schema:
        type: string
        format: uuid
    EncounterTreatmentPlanFilter:
      in: query
      name: treatment_plan_id
      required: false
      schema:
        type: string
        format: uuid
    EncounterAppointmentFilter:
      in: query
      name: appointment_id
      required: false
      schema:
        type: string
        format: uuid
    EncounterClinicFilter:
      in: query
      name: clinic_id
      required: false
      schema:
        type: string
        format: uuid
    EncounterFromFilter:
      in: query
      name: encounter_from
      required: false
      schema:
        type: string
        format: date-time
    EncounterToFilter:
      in: query
      name: encounter_to
      required: false
      schema:
        type: string
        format: date-time
    EncounterCreatedFromFilter:
      in: query
      name: created_from
      required: false
      schema:
        type: string
        format: date
    EncounterCreatedToFilter:
      in: query
      name: created_to
      required: false
      schema:
        type: string
        format: date
    EncounterSearchLimit:
      in: query
      name: limit
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    EncounterExportLimit:
      in: query
      name: limit
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 1000
        default: 1000
    EncounterExportFormat:
      in: query
      name: format
      required: false
      schema:
        type: string
        enum: [csv]
        default: csv
    AppointmentAuditLimit:
      in: query
      name: limit
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 50
    WaitlistStatusFilter:
      in: query
      name: status
      required: false
      schema:
        type: string
        enum: [open, booked, removed]
    WaitlistPatientFilter:
      in: query
      name: patient_id
      required: false
      schema:
        type: string
        format: uuid
    WaitlistProviderFilter:
      in: query
      name: provider_id
      required: false
      schema:
        type: string
        format: uuid
    WaitlistClinicFilter:
      in: query
      name: clinic_id
      required: false
      schema:
        type: string
        format: uuid
    WaitlistDesiredFromFilter:
      in: query
      name: desired_from
      required: false
      schema:
        type: string
        format: date
    WaitlistDesiredToFilter:
      in: query
      name: desired_to
      required: false
      schema:
        type: string
        format: date
    WaitlistLimit:
      in: query
      name: limit
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 50
  schemas:
    ApiErrorResponse:
      type: object
      required: [code, message, details, trace_id, correlation_id]
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: object
          additionalProperties: true
        trace_id:
          type: string
        correlation_id:
          type: string
    TreatmentPlanTransition:
      type: object
      required: [from_status, to_status, occurred_at, actor, reason]
      properties:
        from_status:
          type: string
          enum: [draft, approved, active, paused, finished, rejected]
        to_status:
          type: string
          enum: [draft, approved, active, paused, finished, rejected]
        occurred_at:
          type: string
          format: date-time
        actor:
          type: object
          required: [type, id, name]
          properties:
            type:
              type: string
            id:
              type: string
              nullable: true
            name:
              type: string
              nullable: true
        reason:
          type: string
          nullable: true
    TreatmentPlan:
      type: object
      required:
        - id
        - tenant_id
        - patient
        - provider
        - title
        - status
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        patient:
          $ref: '#/components/schemas/AppointmentReference'
        provider:
          $ref: '#/components/schemas/AppointmentReference'
        title:
          type: string
        summary:
          type: string
          nullable: true
        goals:
          type: string
          nullable: true
        planned_start_date:
          type: string
          format: date
          nullable: true
        planned_end_date:
          type: string
          format: date
          nullable: true
        item_count:
          type: integer
          minimum: 0
        status:
          type: string
          enum: [draft, approved, active, paused, finished, rejected]
        last_transition:
          oneOf:
            - $ref: '#/components/schemas/TreatmentPlanTransition'
            - type: 'null'
        approved_at:
          type: string
          format: date-time
          nullable: true
        started_at:
          type: string
          format: date-time
          nullable: true
        paused_at:
          type: string
          format: date-time
          nullable: true
        finished_at:
          type: string
          format: date-time
          nullable: true
        rejected_at:
          type: string
          format: date-time
          nullable: true
        deleted_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    TreatmentPlanCollectionResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/TreatmentPlan'
    TreatmentPlanSearchResponse:
      type: object
      required: [data, meta]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/TreatmentPlan'
        meta:
          type: object
          required: [filters]
          properties:
            filters:
              $ref: '#/components/schemas/TreatmentPlanSearchFilters'
    TreatmentPlanResponse:
      type: object
      required: [data]
      properties:
        data:
          $ref: '#/components/schemas/TreatmentPlan'
    TreatmentPlanMutationResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum:
            - treatment_plan_created
            - treatment_plan_updated
            - treatment_plan_deleted
            - treatment_plan_approved
            - treatment_plan_started
            - treatment_plan_paused
            - treatment_plan_resumed
            - treatment_plan_finished
            - treatment_plan_rejected
        data:
          $ref: '#/components/schemas/TreatmentPlan'
    TreatmentPlanItem:
      type: object
      required: [id, plan_id, tenant_id, item_type, title, sort_order, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        plan_id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        item_type:
          type: string
          enum: [assessment, procedure, medication, therapy, lab, follow_up, other]
        title:
          type: string
        description:
          type: string
          nullable: true
        instructions:
          type: string
          nullable: true
        sort_order:
          type: integer
          minimum: 1
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    TreatmentPlanItemCollectionResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/TreatmentPlanItem'
    TreatmentPlanItemMutationResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum:
            - treatment_plan_item_created
            - treatment_plan_item_updated
            - treatment_plan_item_deleted
        data:
          $ref: '#/components/schemas/TreatmentPlanItem'
    CreateTreatmentPlanRequest:
      type: object
      required: [patient_id, provider_id, title]
      properties:
        patient_id:
          type: string
          format: uuid
        provider_id:
          type: string
          format: uuid
        title:
          type: string
          maxLength: 255
        summary:
          type: string
          maxLength: 5000
          nullable: true
        goals:
          type: string
          maxLength: 5000
          nullable: true
        planned_start_date:
          type: string
          format: date
          nullable: true
        planned_end_date:
          type: string
          format: date
          nullable: true
    TreatmentPlanSearchFilters:
      type: object
      properties:
        q:
          type: string
          nullable: true
        status:
          type: string
          nullable: true
        patient_id:
          type: string
          format: uuid
          nullable: true
        provider_id:
          type: string
          format: uuid
          nullable: true
        planned_from:
          type: string
          format: date
          nullable: true
        planned_to:
          type: string
          format: date
          nullable: true
        created_from:
          type: string
          format: date
          nullable: true
        created_to:
          type: string
          format: date
          nullable: true
        limit:
          type: integer
    CreateTreatmentPlanItemRequest:
      type: object
      required: [item_type, title]
      properties:
        item_type:
          type: string
          enum: [assessment, procedure, medication, therapy, lab, follow_up, other]
        title:
          type: string
          maxLength: 255
        description:
          type: string
          maxLength: 5000
          nullable: true
        instructions:
          type: string
          maxLength: 5000
          nullable: true
        sort_order:
          type: integer
          minimum: 1
    UpdateTreatmentPlanRequest:
      type: object
      properties:
        patient_id:
          type: string
          format: uuid
        provider_id:
          type: string
          format: uuid
        title:
          type: string
          maxLength: 255
        summary:
          type: string
          maxLength: 5000
          nullable: true
        goals:
          type: string
          maxLength: 5000
          nullable: true
        planned_start_date:
          type: string
          format: date
          nullable: true
        planned_end_date:
          type: string
          format: date
          nullable: true
    UpdateTreatmentPlanItemRequest:
      type: object
      properties:
        item_type:
          type: string
          enum: [assessment, procedure, medication, therapy, lab, follow_up, other]
        title:
          type: string
          maxLength: 255
        description:
          type: string
          maxLength: 5000
          nullable: true
        instructions:
          type: string
          maxLength: 5000
          nullable: true
        sort_order:
          type: integer
          minimum: 1
    TreatmentPlanReasonRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          maxLength: 5000
    Encounter:
      type: object
      required:
        - id
        - tenant_id
        - patient
        - provider
        - status
        - encountered_at
        - timezone
        - diagnosis_count
        - procedure_count
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        patient:
          $ref: '#/components/schemas/AppointmentReference'
        provider:
          $ref: '#/components/schemas/AppointmentReference'
        treatment_plan_id:
          type: string
          format: uuid
          nullable: true
        appointment_id:
          type: string
          format: uuid
          nullable: true
        clinic:
          $ref: '#/components/schemas/AppointmentLocationReference'
        room:
          $ref: '#/components/schemas/AppointmentLocationReference'
        status:
          type: string
          enum: [open, completed, entered_in_error]
        encountered_at:
          type: string
          format: date-time
        timezone:
          type: string
        chief_complaint:
          type: string
          nullable: true
        summary:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        follow_up_instructions:
          type: string
          nullable: true
        diagnosis_count:
          type: integer
          minimum: 0
        procedure_count:
          type: integer
          minimum: 0
        deleted_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    EncounterCollectionResponse:
      type: object
      required: [data, meta]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Encounter'
        meta:
          type: object
          required: [filters]
          properties:
            filters:
              $ref: '#/components/schemas/EncounterSearchFilters'
    EncounterResponse:
      type: object
      required: [data]
      properties:
        data:
          $ref: '#/components/schemas/Encounter'
    EncounterMutationResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [encounter_created, encounter_updated, encounter_deleted]
        data:
          $ref: '#/components/schemas/Encounter'
    EncounterSearchFilters:
      type: object
      properties:
        q:
          type: string
          nullable: true
        status:
          type: string
          nullable: true
        patient_id:
          type: string
          format: uuid
          nullable: true
        provider_id:
          type: string
          format: uuid
          nullable: true
        treatment_plan_id:
          type: string
          format: uuid
          nullable: true
        appointment_id:
          type: string
          format: uuid
          nullable: true
        clinic_id:
          type: string
          format: uuid
          nullable: true
        encounter_from:
          type: string
          format: date-time
          nullable: true
        encounter_to:
          type: string
          format: date-time
          nullable: true
        created_from:
          type: string
          format: date
          nullable: true
        created_to:
          type: string
          format: date
          nullable: true
        limit:
          type: integer
    EncounterExport:
      type: object
      required: [export_id, format, file_name, row_count, generated_at, filters, storage]
      properties:
        export_id:
          type: string
          format: uuid
        format:
          type: string
          enum: [csv]
        file_name:
          type: string
        row_count:
          type: integer
          minimum: 0
        generated_at:
          type: string
          format: date-time
        filters:
          $ref: '#/components/schemas/EncounterSearchFilters'
        storage:
          type: object
          required: [disk, path, visibility]
          properties:
            disk:
              type: string
            path:
              type: string
            visibility:
              type: string
    EncounterExportResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [encounter_export_created]
        data:
          $ref: '#/components/schemas/EncounterExport'
    EncounterDiagnosis:
      type: object
      required: [id, tenant_id, encounter_id, display_name, diagnosis_type, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        encounter_id:
          type: string
          format: uuid
        code:
          type: string
          nullable: true
        display_name:
          type: string
        diagnosis_type:
          type: string
          enum: [primary, secondary]
        notes:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    EncounterDiagnosisCollectionResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/EncounterDiagnosis'
    EncounterDiagnosisMutationResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [encounter_diagnosis_added, encounter_diagnosis_removed]
        data:
          $ref: '#/components/schemas/EncounterDiagnosis'
    EncounterProcedure:
      type: object
      required: [id, tenant_id, encounter_id, display_name, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        encounter_id:
          type: string
          format: uuid
        treatment_item_id:
          type: string
          format: uuid
          nullable: true
        code:
          type: string
          nullable: true
        display_name:
          type: string
        performed_at:
          type: string
          format: date-time
          nullable: true
        notes:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    EncounterProcedureCollectionResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/EncounterProcedure'
    EncounterProcedureMutationResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [encounter_procedure_added, encounter_procedure_removed]
        data:
          $ref: '#/components/schemas/EncounterProcedure'
    CreateEncounterRequest:
      type: object
      required: [patient_id, provider_id, encountered_at, timezone]
      properties:
        patient_id:
          type: string
          format: uuid
        provider_id:
          type: string
          format: uuid
        treatment_plan_id:
          type: string
          format: uuid
          nullable: true
        appointment_id:
          type: string
          format: uuid
          nullable: true
        clinic_id:
          type: string
          format: uuid
          nullable: true
        room_id:
          type: string
          format: uuid
          nullable: true
        encountered_at:
          type: string
          format: date-time
        timezone:
          type: string
        chief_complaint:
          type: string
          maxLength: 255
          nullable: true
        summary:
          type: string
          maxLength: 5000
          nullable: true
        notes:
          type: string
          maxLength: 10000
          nullable: true
        follow_up_instructions:
          type: string
          maxLength: 5000
          nullable: true
    UpdateEncounterRequest:
      type: object
      properties:
        patient_id:
          type: string
          format: uuid
        provider_id:
          type: string
          format: uuid
        treatment_plan_id:
          type: string
          format: uuid
          nullable: true
        appointment_id:
          type: string
          format: uuid
          nullable: true
        clinic_id:
          type: string
          format: uuid
          nullable: true
        room_id:
          type: string
          format: uuid
          nullable: true
        status:
          type: string
          enum: [open, completed, entered_in_error]
        encountered_at:
          type: string
          format: date-time
        timezone:
          type: string
        chief_complaint:
          type: string
          maxLength: 255
          nullable: true
        summary:
          type: string
          maxLength: 5000
          nullable: true
        notes:
          type: string
          maxLength: 10000
          nullable: true
        follow_up_instructions:
          type: string
          maxLength: 5000
          nullable: true
    CreateEncounterDiagnosisRequest:
      type: object
      required: [display_name, diagnosis_type]
      properties:
        code:
          type: string
          maxLength: 32
          nullable: true
        display_name:
          type: string
          maxLength: 255
        diagnosis_type:
          type: string
          enum: [primary, secondary]
        notes:
          type: string
          maxLength: 5000
          nullable: true
    CreateEncounterProcedureRequest:
      type: object
      required: [display_name]
      properties:
        treatment_item_id:
          type: string
          format: uuid
          nullable: true
        code:
          type: string
          maxLength: 32
          nullable: true
        display_name:
          type: string
          maxLength: 255
        performed_at:
          type: string
          format: date-time
          nullable: true
        notes:
          type: string
          maxLength: 5000
          nullable: true
    BulkEncounterChanges:
      type: object
      properties:
        status:
          type: string
          enum: [open, completed, entered_in_error]
        provider_id:
          type: string
          format: uuid
        clinic_id:
          type: string
          format: uuid
          nullable: true
        room_id:
          type: string
          format: uuid
          nullable: true
        encountered_at:
          type: string
          format: date-time
        timezone:
          type: string
    BulkUpdateEncountersRequest:
      type: object
      required: [encounter_ids, changes]
      properties:
        encounter_ids:
          type: array
          minItems: 1
          maxItems: 100
          items:
            type: string
            format: uuid
        changes:
          $ref: '#/components/schemas/BulkEncounterChanges'
    BulkEncounterUpdateResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [encounters_bulk_updated]
        data:
          type: object
          required: [operation_id, affected_count, updated_fields, encounters]
          properties:
            operation_id:
              type: string
              format: uuid
            affected_count:
              type: integer
              minimum: 0
            updated_fields:
              type: array
              items:
                type: string
            encounters:
              type: array
              items:
                $ref: '#/components/schemas/Encounter'
    AppointmentReference:
      type: object
      required: [id, display_name]
      properties:
        id:
          type: string
          format: uuid
        display_name:
          type: string
    AppointmentLocationReference:
      type: object
      nullable: true
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
    AppointmentActor:
      type: object
      required: [type]
      properties:
        type:
          type: string
        id:
          type: string
          nullable: true
        name:
          type: string
          nullable: true
    AppointmentSlot:
      type: object
      required: [start_at, end_at, timezone]
      properties:
        start_at:
          type: string
          format: date-time
        end_at:
          type: string
          format: date-time
        timezone:
          type: string
    AppointmentTransition:
      type: object
      nullable: true
      properties:
        from_status:
          type: string
        to_status:
          type: string
        occurred_at:
          type: string
          format: date-time
        actor:
          $ref: '#/components/schemas/AppointmentActor'
        reason:
          type: string
          nullable: true
        admin_override:
          type: boolean
        restored_from_status:
          type: string
          nullable: true
        replacement_appointment_id:
          type: string
          format: uuid
          nullable: true
        replacement_slot:
          oneOf:
            - $ref: '#/components/schemas/AppointmentSlot'
            - type: 'null'
    Appointment:
      type: object
      required:
        - id
        - tenant_id
        - status
        - patient
        - provider
        - scheduled_slot
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [draft, scheduled, confirmed, checked_in, in_progress, completed, canceled, no_show, rescheduled]
        patient:
          $ref: '#/components/schemas/AppointmentReference'
        provider:
          $ref: '#/components/schemas/AppointmentReference'
        clinic:
          $ref: '#/components/schemas/AppointmentLocationReference'
        room:
          $ref: '#/components/schemas/AppointmentLocationReference'
        scheduled_slot:
          $ref: '#/components/schemas/AppointmentSlot'
        last_transition:
          $ref: '#/components/schemas/AppointmentTransition'
        deleted_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    AppointmentResponse:
      type: object
      required: [data]
      properties:
        data:
          $ref: '#/components/schemas/Appointment'
    AppointmentCollectionResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Appointment'
    AppointmentMutationResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [appointment_created, appointment_updated, appointment_deleted]
        data:
          $ref: '#/components/schemas/Appointment'
    AppointmentWorkflowMutationResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum:
            - appointment_scheduled
            - appointment_confirmed
            - appointment_checked_in
            - appointment_started
            - appointment_completed
            - appointment_canceled
            - appointment_no_show
            - appointment_restored
        data:
          $ref: '#/components/schemas/Appointment'
    AppointmentReschedule:
      type: object
      required: [appointment, replacement_appointment]
      properties:
        appointment:
          $ref: '#/components/schemas/Appointment'
        replacement_appointment:
          $ref: '#/components/schemas/Appointment'
    AppointmentRescheduleResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [appointment_rescheduled]
        data:
          $ref: '#/components/schemas/AppointmentReschedule'
    AppointmentRecurrence:
      type: object
      required:
        - id
        - source_appointment_id
        - patient_id
        - provider_id
        - frequency
        - interval
        - timezone
        - status
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        source_appointment_id:
          type: string
          format: uuid
        patient_id:
          type: string
          format: uuid
        provider_id:
          type: string
          format: uuid
        clinic_id:
          type: string
          format: uuid
          nullable: true
        room_id:
          type: string
          format: uuid
          nullable: true
        frequency:
          type: string
          enum: [daily, weekly, monthly]
        interval:
          type: integer
        occurrence_count:
          type: integer
          nullable: true
        until_date:
          type: string
          format: date
          nullable: true
        timezone:
          type: string
        status:
          type: string
          enum: [active, canceled]
        canceled_reason:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    AppointmentRecurrenceMaterialization:
      type: object
      required: [recurrence, appointments]
      properties:
        recurrence:
          $ref: '#/components/schemas/AppointmentRecurrence'
        appointments:
          type: array
          items:
            $ref: '#/components/schemas/Appointment'
    AppointmentRecurrenceMaterializationResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [appointment_recurrence_created]
        data:
          $ref: '#/components/schemas/AppointmentRecurrenceMaterialization'
    AppointmentRecurrenceMutationResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [appointment_recurrence_canceled]
        data:
          $ref: '#/components/schemas/AppointmentRecurrence'
    AppointmentParticipant:
      type: object
      required:
        - id
        - appointment_id
        - participant_type
        - display_name
        - role
        - required
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        appointment_id:
          type: string
          format: uuid
        participant_type:
          type: string
          enum: [user, provider, external]
        reference_id:
          type: string
          format: uuid
          nullable: true
        display_name:
          type: string
        role:
          type: string
        required:
          type: boolean
        notes:
          type: string
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    AppointmentParticipantCollectionResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AppointmentParticipant'
    AppointmentParticipantMutationResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [appointment_participant_created, appointment_participant_deleted]
        data:
          $ref: '#/components/schemas/AppointmentParticipant'
    AppointmentNoteAuthor:
      type: object
      required: [user_id, name, email]
      properties:
        user_id:
          type: string
          format: uuid
        name:
          type: string
        email:
          type: string
          format: email
    AppointmentNote:
      type: object
      required:
        - id
        - appointment_id
        - body
        - author
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        appointment_id:
          type: string
          format: uuid
        body:
          type: string
        author:
          $ref: '#/components/schemas/AppointmentNoteAuthor'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    AppointmentNoteCollectionResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AppointmentNote'
    AppointmentNoteMutationResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [appointment_note_created, appointment_note_updated, appointment_note_deleted]
        data:
          $ref: '#/components/schemas/AppointmentNote'
    BulkAppointmentUpdate:
      type: object
      required: [operation_id, affected_count, updated_fields, appointments]
      properties:
        operation_id:
          type: string
          format: uuid
        affected_count:
          type: integer
        updated_fields:
          type: array
          items:
            type: string
            enum: [patient_id, provider_id, clinic_id, room_id, scheduled_start_at, scheduled_end_at, timezone]
        appointments:
          type: array
          items:
            $ref: '#/components/schemas/Appointment'
    BulkAppointmentUpdateResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [appointments_bulk_updated]
        data:
          $ref: '#/components/schemas/BulkAppointmentUpdate'
    BulkAppointmentTransition:
      type: object
      required: [operation_id, affected_count, appointments, replacement_appointments]
      properties:
        operation_id:
          type: string
          format: uuid
        affected_count:
          type: integer
        appointments:
          type: array
          items:
            $ref: '#/components/schemas/Appointment'
        replacement_appointments:
          type: array
          items:
            $ref: '#/components/schemas/Appointment'
    BulkAppointmentTransitionResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [appointments_bulk_canceled, appointments_bulk_rescheduled]
        data:
          $ref: '#/components/schemas/BulkAppointmentTransition'
    AppointmentSearchFilters:
      type: object
      properties:
        q:
          type: string
          nullable: true
        status:
          type: string
          nullable: true
        patient_id:
          type: string
          format: uuid
          nullable: true
        provider_id:
          type: string
          format: uuid
          nullable: true
        clinic_id:
          type: string
          format: uuid
          nullable: true
        room_id:
          type: string
          format: uuid
          nullable: true
        scheduled_from:
          type: string
          format: date
          nullable: true
        scheduled_to:
          type: string
          format: date
          nullable: true
        created_from:
          type: string
          format: date
          nullable: true
        created_to:
          type: string
          format: date
          nullable: true
        limit:
          type: integer
    AppointmentSearchResponse:
      type: object
      required: [data, meta]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/Appointment'
        meta:
          type: object
          required: [filters]
          properties:
            filters:
              $ref: '#/components/schemas/AppointmentSearchFilters'
    AppointmentExport:
      type: object
      required:
        - export_id
        - format
        - file_name
        - row_count
        - generated_at
        - filters
        - storage
      properties:
        export_id:
          type: string
          format: uuid
        format:
          type: string
          enum: [csv]
        file_name:
          type: string
        row_count:
          type: integer
        generated_at:
          type: string
          format: date-time
        filters:
          $ref: '#/components/schemas/AppointmentSearchFilters'
        storage:
          type: object
          required: [disk, path, visibility]
          properties:
            disk:
              type: string
            path:
              type: string
            visibility:
              type: string
    AppointmentExportResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [appointment_export_created]
        data:
          $ref: '#/components/schemas/AppointmentExport'
    AppointmentAuditEvent:
      type: object
      required:
        - id
        - action
        - object_type
        - object_id
        - actor
        - request_id
        - correlation_id
        - before
        - after
        - metadata
        - occurred_at
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
          nullable: true
        action:
          type: string
        object_type:
          type: string
        object_id:
          type: string
        actor:
          type: object
          required: [type]
          properties:
            type:
              type: string
            id:
              type: string
              nullable: true
            name:
              type: string
              nullable: true
        request_id:
          type: string
          format: uuid
        correlation_id:
          type: string
          format: uuid
        before:
          type: object
          additionalProperties: true
        after:
          type: object
          additionalProperties: true
        metadata:
          type: object
          additionalProperties: true
        occurred_at:
          type: string
          format: date-time
    AppointmentAuditResponse:
      type: object
      required: [data, meta]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/AppointmentAuditEvent'
        meta:
          type: object
          required: [limit]
          properties:
            limit:
              type: integer
    WaitlistEntry:
      type: object
      required:
        - id
        - patient
        - provider
        - desired_date_from
        - desired_date_to
        - status
        - created_at
        - updated_at
      properties:
        id:
          type: string
          format: uuid
        patient:
          $ref: '#/components/schemas/AppointmentReference'
        provider:
          $ref: '#/components/schemas/AppointmentReference'
        clinic:
          $ref: '#/components/schemas/AppointmentLocationReference'
        room:
          $ref: '#/components/schemas/AppointmentLocationReference'
        desired_date_from:
          type: string
          format: date
        desired_date_to:
          type: string
          format: date
        preferred_start_time:
          type: string
          nullable: true
        preferred_end_time:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        status:
          type: string
          enum: [open, booked, removed]
        booked_appointment_id:
          type: string
          format: uuid
          nullable: true
        offered_slot:
          oneOf:
            - $ref: '#/components/schemas/AppointmentSlot'
            - type: 'null'
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    WaitlistCollectionResponse:
      type: object
      required: [data, meta]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/WaitlistEntry'
        meta:
          type: object
          required: [filters]
          properties:
            filters:
              $ref: '#/components/schemas/WaitlistFilters'
    WaitlistMutationResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [waitlist_entry_created, waitlist_entry_removed]
        data:
          $ref: '#/components/schemas/WaitlistEntry'
    WaitlistOffer:
      type: object
      required: [entry, appointment]
      properties:
        entry:
          $ref: '#/components/schemas/WaitlistEntry'
        appointment:
          $ref: '#/components/schemas/Appointment'
    WaitlistOfferResponse:
      type: object
      required: [status, data]
      properties:
        status:
          type: string
          enum: [waitlist_slot_offered]
        data:
          $ref: '#/components/schemas/WaitlistOffer'
    WaitlistFilters:
      type: object
      properties:
        status:
          type: string
          nullable: true
        patient_id:
          type: string
          format: uuid
          nullable: true
        provider_id:
          type: string
          format: uuid
          nullable: true
        clinic_id:
          type: string
          format: uuid
          nullable: true
        desired_from:
          type: string
          format: date
          nullable: true
        desired_to:
          type: string
          format: date
          nullable: true
        limit:
          type: integer
    CreateAppointmentRequest:
      type: object
      required:
        - patient_id
        - provider_id
        - scheduled_start_at
        - scheduled_end_at
        - timezone
      properties:
        patient_id:
          type: string
          format: uuid
        provider_id:
          type: string
          format: uuid
        clinic_id:
          type: string
          format: uuid
          nullable: true
        room_id:
          type: string
          format: uuid
          nullable: true
        scheduled_start_at:
          type: string
          format: date-time
        scheduled_end_at:
          type: string
          format: date-time
        timezone:
          type: string
    UpdateAppointmentRequest:
      type: object
      properties:
        patient_id:
          type: string
          format: uuid
        provider_id:
          type: string
          format: uuid
        clinic_id:
          type: string
          format: uuid
          nullable: true
        room_id:
          type: string
          format: uuid
          nullable: true
        scheduled_start_at:
          type: string
          format: date-time
        scheduled_end_at:
          type: string
          format: date-time
        timezone:
          type: string
    CreateAppointmentParticipantRequest:
      type: object
      required: [participant_type, role]
      properties:
        participant_type:
          type: string
          enum: [user, provider, external]
        reference_id:
          type: string
          format: uuid
          nullable: true
        display_name:
          type: string
          maxLength: 160
          nullable: true
        role:
          type: string
          maxLength: 120
        required:
          type: boolean
          default: false
        notes:
          type: string
          maxLength: 5000
          nullable: true
    CreateAppointmentNoteRequest:
      type: object
      required: [body]
      properties:
        body:
          type: string
          maxLength: 10000
    UpdateAppointmentNoteRequest:
      type: object
      properties:
        body:
          type: string
          maxLength: 10000
    AppointmentCheckInRequest:
      type: object
      properties:
        admin_override:
          type: boolean
          default: false
    AppointmentReasonRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
          maxLength: 5000
    AppointmentRescheduleRequest:
      type: object
      required: [reason, replacement_start_at, replacement_end_at, timezone]
      properties:
        reason:
          type: string
          maxLength: 5000
        replacement_start_at:
          type: string
          format: date-time
        replacement_end_at:
          type: string
          format: date-time
        timezone:
          type: string
        clinic_id:
          type: string
          format: uuid
          nullable: true
        room_id:
          type: string
          format: uuid
          nullable: true
    RecurringAppointmentRequest:
      type: object
      required: [frequency, interval]
      properties:
        frequency:
          type: string
          enum: [daily, weekly, monthly]
        interval:
          type: integer
          minimum: 1
          maximum: 12
        occurrence_count:
          type: integer
          minimum: 2
          maximum: 24
          nullable: true
        until_date:
          type: string
          format: date
          nullable: true
    BulkUpdateAppointmentsRequest:
      type: object
      required: [appointment_ids, changes]
      properties:
        appointment_ids:
          type: array
          minItems: 1
          maxItems: 100
          uniqueItems: true
          items:
            type: string
            format: uuid
        changes:
          type: object
          minProperties: 1
          properties:
            patient_id:
              type: string
              format: uuid
            provider_id:
              type: string
              format: uuid
            clinic_id:
              oneOf:
                - type: string
                  format: uuid
                - type: 'null'
            room_id:
              oneOf:
                - type: string
                  format: uuid
                - type: 'null'
            scheduled_start_at:
              type: string
              format: date-time
            scheduled_end_at:
              type: string
              format: date-time
            timezone:
              type: string
    BulkCancelAppointmentsRequest:
      type: object
      required: [appointment_ids, reason]
      properties:
        appointment_ids:
          type: array
          minItems: 1
          maxItems: 100
          uniqueItems: true
          items:
            type: string
            format: uuid
        reason:
          type: string
          maxLength: 5000
    BulkRescheduleAppointmentsRequest:
      type: object
      required: [items]
      properties:
        items:
          type: array
          minItems: 1
          maxItems: 100
          items:
            type: object
            required:
              - appointment_id
              - reason
              - replacement_start_at
              - replacement_end_at
              - timezone
            properties:
              appointment_id:
                type: string
                format: uuid
              reason:
                type: string
                maxLength: 5000
              replacement_start_at:
                type: string
                format: date-time
              replacement_end_at:
                type: string
                format: date-time
              timezone:
                type: string
              clinic_id:
                type: string
                format: uuid
                nullable: true
              room_id:
                type: string
                format: uuid
                nullable: true
    WaitlistCreateRequest:
      type: object
      required:
        - patient_id
        - provider_id
        - desired_date_from
        - desired_date_to
      properties:
        patient_id:
          type: string
          format: uuid
        provider_id:
          type: string
          format: uuid
        clinic_id:
          type: string
          format: uuid
          nullable: true
        room_id:
          type: string
          format: uuid
          nullable: true
        desired_date_from:
          type: string
          format: date
        desired_date_to:
          type: string
          format: date
        preferred_start_time:
          type: string
          nullable: true
        preferred_end_time:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
    WaitlistOfferRequest:
      type: object
      required: [scheduled_start_at, scheduled_end_at, timezone]
      properties:
        scheduled_start_at:
          type: string
          format: date-time
        scheduled_end_at:
          type: string
          format: date-time
        timezone:
          type: string
        clinic_id:
          type: string
          format: uuid
          nullable: true
        room_id:
          type: string
          format: uuid
          nullable: true
